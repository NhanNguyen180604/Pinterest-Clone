<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.admin.manage_comment.ManageCommentFragment">

    <!-- Thanh tìm kiếm & bộ lọc -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/inner_comment_management_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:background="@drawable/custom_search_view_background"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <androidx.appcompat.widget.SearchView
            android:id="@+id/comment_management_search_bar"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:inputType="text"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            app:closeIcon="@drawable/ic_search_cancel"
            app:iconifiedByDefault="false"
            app:layout_constraintEnd_toStartOf="@+id/btn_comments_filter"
            app:layout_constraintStart_toStartOf="parent"
            app:queryBackground="@null"
            app:queryHint="Search for users"
            app:searchIcon="@drawable/ic_search"
            tools:ignore="MissingConstraints" />

        <ImageButton
            android:id="@+id/btn_comments_filter"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@null"
            android:contentDescription="TODO"
            android:padding="8dp"
            android:src="@drawable/ic_filter"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

<!--     Danh sách comment người dùng -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_comments_list"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@id/inner_comment_management_container"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <!-- ProgressBar tải dữ liệu -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/inner_comment_management_container"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>


</androidx.constraintlayout.widget.ConstraintLayout>
